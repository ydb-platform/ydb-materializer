1. Написать тесты на удаление:
  - удаление из самой левой таблицы MV
  - удаление из промежуточной таблицы, соединённой с основной через INNER
  - удаление из промежуточной таблицы, соединённой с основной через LEFT
  - удаление из справочника, соединённого с основной таблицей через INNER
  - удаление из справочника, соединённого с основной таблицей через LEFT

2. Написать тесты на удаление в варианте, когда PK MV является вычисляемым
  - реализовать логику удаления для вычисляемого варианта PK MV (сейчас такой вариант не работает)
  - в том числе на составной PK

3. Научить материализатор:
  - работать с некоррелированными подзапросами в JOIN (условие внутри + LEFT)
  - работать с LEFT JOIN ANY

4. (DONE) Разобраться с захватом лидера
 - runner не реагирует на удаление координатором
 - координатор может удалить сам себя через очистку runner, после чего новый координатор не появляется
